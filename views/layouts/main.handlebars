<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    {{#if articleEntry}}
    <link href="https://cdn.quilljs.com/1.0.0/quill.snow.css" rel="stylesheet">
    {{/if}}
    {{#if title}}
    <title>{{title}}</title>
    {{else}}
    <title>Default title.</title>
    {{/if}}
</head>

<body>
    {{{body}}}
</body>

</html>